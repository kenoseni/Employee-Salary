import chai from 'chai'
const expect = chai.expect

describe('', () => {
    describe('', () => {

      it('', () => {
        expect().to.be.equal()
      });
      it('', () => {
        expect().to.be.equal()
      });
      it('', () => {
        expect().to.be.equal()
      });
      
    });
})